# CODING MCP 主控台瀏覽器擴充功能

1. 開啟 Chrome 的 `chrome://extensions` 或 Edge 的 `edge://extensions`。
2. 啟用「開發人員模式」。
3. 選擇「載入未封裝項目」，指定這個 `browser-extension` 資料夾。
4. 重新整理要使用的網頁；頁面右側會出現 `CODING MCP` 啟動鈕。

擴充功能只會連到 `http://127.0.0.1:8768`，工作紀錄與 HUMAN_HELP 內容不會送往外部網站。
